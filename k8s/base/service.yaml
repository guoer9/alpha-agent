apiVersion: v1
kind: Service
metadata:
  name: qwen-vllm
  labels:
    app: qwen-vllm
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: qwen-vllm
